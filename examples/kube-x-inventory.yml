# k3s_cluster can't be changed (hardcoded in dependent package)
k3s_cluster:
  vars:
    ans_x_admin_username: admin
    ans_x_admin_public_key: ssh-rsa AAAAB3NzaC1yxxxx
    ans_x_k3s_token: jbHWvQv9261KblczY7BX+OLcnZGrMSe+0UiFS3h7Ozc=
    ans_x_k3s_version: v1.31.2+k3s1
    api_endpoint: "{{ hostvars[groups['server'][0]]['ansible_host'] | default(groups['server'][0]) }}"
    extra_server_args:
      - "--disable=traefik"
  children:
    # server name can't be changed (hardcoded in dependent package)
    server:
      # An odd number of server nodes is required (3,5,7).
      # https://docs.k3s.io/datastore/ha-embedded
      hosts:
        kube_server_01:
          ans_x_hostname: kube.example.com
#    agent:
#      hosts:
#        192.16.35.12:
#        192.16.35.13: